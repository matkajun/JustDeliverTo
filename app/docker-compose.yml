#RonBs

version: "3.3"
services:
  app:
    build: ./flask-app
    ports:
      - "4001:4001"
 
  ngnixg:
    build: ./proxy
    ports:
      - "80:80" 
      - "443:443"
   
        #    volumes: 
        #      -  "./conf/:/etc/nginx/"
        #       -  "./ssl/:/etc/nginx/ssl"
      
  

